# Aura releases

Public manifest + APK assets for sideloaded JL เลขา (Aura Secretary) OTA updates.

- `version.json` — checked by the app at launch / Settings → อัปเดตแอป
- APK files — attached to GitHub Releases (not stored in git)

Source code lives in a private repo; this repo is releases-only.
